(library
 ((name        metrics_gnuplot)
  (public_name metrics-unix)
  (wrapped     false)
  (modules     (metrics_gnuplot))
  (libraries   (metrics fmt uuidm unix mtime mtime.clock mtime.clock.os))))
